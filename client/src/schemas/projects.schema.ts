export interface Project {
  id: string;
  name: string;
  subDomain?: string;
  customDomain?: string;
  faviconUrl?: string;
  description?: string;
  // createdAt: Date;
  // updatedAt: Date;
  // userId: string;
}
