export type PF_STAT = {
    title: string;
    value: string;
};
export type PF_ABOUT_SECTION = {
    heading: string;
    title: string;
    colorTitle: string;
    description: string;
    stats: PF_STAT[];
};
