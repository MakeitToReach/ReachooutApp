export type PF_PROJECT = {
    heading: string;
    // subtitle: string;
    imgUrl?: string | string[];
    vidUrl?: string;
    description: string;
    btnText: string;
    btnLink: string;
    category: string;
};
